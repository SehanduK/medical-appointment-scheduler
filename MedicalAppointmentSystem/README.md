Medical Record Management Module (Spring Boot)

Run locally:

1. Build and run with Maven:

```bash
mvn spring-boot:run
```

2. Open http://localhost:8080/records

Notes:
- Records are stored in `src/main/resources/data/records.txt` (file-based storage for demo)
- Java 17
- Simple Bootstrap + Thymeleaf UI
